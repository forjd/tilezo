{
  "name": "@tilezo/server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "@tilezo/engine": "workspace:*",
    "@tilezo/protocol": "workspace:*",
    "drizzle-orm": "^0.45.2"
  },
  "scripts": {
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:studio": "drizzle-kit studio",
    "dev": "bun run ../../scripts/run-server-dev.ts",
    "format": "biome format --write .",
    "lint": "biome check .",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "drizzle-kit": "^0.31.10",
    "postgres": "^3.4.9"
  }
}
