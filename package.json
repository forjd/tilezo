{
  "name": "habbo-like-mvp",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --filter '@habbo/server' --filter '@habbo/client' --parallel dev",
    "dev:client": "bun run --filter '@habbo/client' dev",
    "dev:server": "bun run --filter '@habbo/server' dev",
    "format": "biome format --write .",
    "lint": "biome check .",
    "test": "bun test",
    "typecheck": "bun run --filter '*' typecheck"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.0.0",
    "bun-types": "^1.3.13",
    "typescript": "^5.8.3"
  }
}
