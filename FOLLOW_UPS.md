# Follow Ups

## Create Room Access Rules

The create-room flow persists the selected `access` setting, including `knock`, but the room join path does not yet enforce a full knock/access approval flow. Before marking the Create Room Dialog idea as fully implemented, add server-side access checks, client feedback for blocked joins, and tests that cover open versus knock-only rooms.
